# src/sectors/commute/ — 폴더 가이드

## 역할

이동 패턴을 소유한다. 위치 자동화가 주는 **사건의 시각**을 관측치로 삼는다.

**이 폴더가 소유하지 않는 것**: 접는 계산(→ `core/folding`), 판단 실행(→ `brain`),
다른 섹터. health 의 수면 데이터를 참조하지 않는다.

---

## 핵심 패턴

### 값이 곧 시각이다
```
17.5  →  오후 5시 30분
```
**이유**: 수면·심박은 측정 표본이지만 퇴근은 **사건**이다. 측정값을 집계하는 게
아니라 "언제 일어났나"가 값이다.

### 하루에 여러 번이면 하나만 고른다
```
퇴근      Fold.LAST    나갔다 들어와도 마지막이 퇴근
출근 도착  Fold.FIRST   처음 도착이 출근
```
**이유**: 평균을 내면 3시와 7시 사이 어딘가라는, **실제로 일어나지 않은 시각**이 나온다.

---

## 금지사항

### ❌ 절대 기준으로 야근을 판정하지 않는다
```python
# ❌ 금지
if departure > 19.0: alert("야근")
```
**사고 이력**: 수면에서 이미 겪었다 (2026-08-21, "7시간 미만" 기준이면 88%의 날에 알림).
6시 퇴근이 평범한 사람과 9시 퇴근이 평범한 사람에게 같은 기준을 대면 한쪽은 매일
울리고 다른 쪽은 영영 안 울린다.

### ❌ 주말을 baseline 에 넣지 않는다
**이유**: 주 5일 패턴을 가진 사람의 "평소"는 평일로만 만들어야 한다.
주말 기록 하나가 평균을 몇 시간씩 끌어내린다.

### ❌ 끊김 기준을 수면과 같게 두지 않는다
```python
stale_after = timedelta(days=4)   # 수면은 36시간
```
**이유**: 퇴근은 하루 한 번뿐인 사건이고 주말·휴가·재택에 자연히 빈다.
36시간 기준이면 **매주 월요일마다** 수집이 멈췄다고 잔소리한다.

---

## GC 패턴

```gc
pattern: "from src\.sectors\.(?!commute)"
message: "섹터는 다른 섹터를 모른다"
```

```gc
pattern: "if\s+\w*departure\w*\s*[<>]=?\s*\d"
message: "절대 시각으로 판정하지 않는다 — 본인 패턴 대비 편차로"
```

---

## 하네스

```
tests:
  - tests/unit/test_commute_trigger.py
```

```bash
python scripts/harness.py src/sectors/commute/
```

---

## 모듈 지도

| 파일 | 책임 |
|------|------|
| `metrics.py` | 퇴근·출근 도착 시각 카드 |
| `triggers.py` | 평소보다 늦은 퇴근 감지 |

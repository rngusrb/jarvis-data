# TASKS — 현재 스프린트

> 최대 3개. 완료된 것은 본문을 지우고 `docs/sprints/` 로 아카이브한다.
> (완료 본문이 남아 있으면 `harness all` 이 FAIL 한다)

## JAR-004: 텔레그램 실전 배선

**목표**: 자비스 발화가 터미널이 아니라 실제 폰으로 온다.

- `@BotFather` 토큰 발급 → `.env` 의 `JARVIS_TELEGRAM_BOT_TOKEN`·`CHAT_ID`
- 실제 발송 1회 확인
- `src/channels/` 전용 테스트 추가 (현재 `test_loop` 가 간접 검증만 한다)

**제약**: 토큰이 URL 경로에 들어간다. 요청 URL 을 로깅하지 않는다.

---

## JAR-005: 발화 기억을 저장소로

**목표**: 서버를 재시작해도 자비스가 했던 말을 잊지 않는다.

현재 `SpeechLog` 가 메모리에만 있어 재시작마다 쿨다운이 초기화되고 같은 잔소리를 반복한다.
개발 중 재시작이 잦아 실제로 겪고 있다.

- `src/brain/memory.py` 의 `SpeechLog` 를 저장소 기반으로
- 발화 → 반응 기록 자리도 함께 마련 (섹터 자기확장의 평가 신호가 된다)

---

## JAR-006: 이동관리 섹터 착수

**목표**: 두 번째 섹터를 만들어 골조가 실제로 버티는지 확인한다.

- 아이폰 단축어 **위치 자동화** (직장 도착/출발) → `/ingest/*`
- `src/sectors/commute/` — 카드 + 트리거
- **플랫폼을 한 줄도 고치지 않고** 되는지가 이 태스크의 진짜 검증

**제약**: 섹터는 다른 섹터를 import 하지 않는다.

# jarvis-data — 작업 지침

> 이 파일은 매 세션 자동 로드된다. **짧게 유지한다.**
> 설명·아키텍처는 DEV_GUIDE.md 로, 현재 할 일은 TASKS.md 로 보낸다.

초개인화 AI 에이전트("자비스"). 애플 생태계에서 개인 데이터를 자동 수집하고,
패턴을 감지해 **먼저 말을 건다.**

## 문서 계층

| 파일 | 역할 |
|------|------|
| **CLAUDE.md** (이 파일) | 핵심 원칙 + 완료 스프린트 목록 |
| **DEV_GUIDE.md** | 아키텍처 지도, "X를 바꾸려면 어디 봐라" 색인 |
| **WORKFLOW.md** | 태스크 lifecycle, 역할, 심각도 기준 |
| **TASKS.md** | 현재 스프린트 (최대 3개) |
| **BACKLOG.md** | 발견된 이슈 + 다음 스프린트 대기열 |
| **각폴더/_GUIDE.md** | 폴더별 금지사항 + 사고 이력 (설명서 아님) |
| **docs/sprints/** | 완료 스프린트 아카이브 |

**작업 시작 전 반드시**: DEV_GUIDE.md → TASKS.md → 해당 폴더/_GUIDE.md

---

## 핵심 원칙

- **플랫폼은 섹터를 모른다.** 방향은 섹터→플랫폼 하나뿐. 섹터를 고르는 곳은 `app/main.py` 하나.
  `tests/invariants/test_boundaries.py` 가 집행한다.
- **같은 계산은 한 곳에만.** 백필과 수신구가 다른 코드를 지나가면 경로에 따라 값이 달라지고,
  원인이 데이터가 아니라 코드 위치라 추적이 지독히 어렵다.
- **이상 감지는 절대 기준이 아니라 개인 baseline 대비 편차.**
- **싼 게이트 먼저, 비싼 LLM 나중.** 알림 스팸이 이 프로덕트의 주된 실패 방식.
- **Silent Failure 금지.** `except Exception: pass`, 빈 값 반환, 로그 없는 fallback —
  조용한 성공보다 시끄러운 실패.
- **"완료" 기준**: `python scripts/harness.py all` 통과.
  pytest 단독 실행은 완료로 **인정하지 않는다** (정적 검사·Doc Lint 누락).
  단축 명령: `sh scripts/verify.sh`
- **같은 실패 2회 연속이면 중단하고 보고.** 테스트를 통과시키려고 테스트를 고치지 않는다.
- **규칙은 사고에서만 생긴다.** 금지사항을 추가할 땐 반드시 **사고 이력**을 같이 적는다.
  이유 없는 규칙은 다음 사람이 지운다.

---

## 인프라 (데스크탑 서버 — Ubuntu, RTX 3090 ×2)

| 서비스 | 포트 |
|--------|------|
| vLLM (Qwen3.8-27B-FP8, TP=2, `--reasoning-parser qwen3`) | 8000 |
| Qdrant (추후 텍스트용) | 6333 |
| 자비스 서버 (맥) | 8100 |

Tailscale — 데스크탑 `100.98.90.38` / 맥 `100.65.255.109`

---

## 두 가지 작업 프로토콜

**단순** (버그픽스, 단일 파일):
```
_GUIDE.md 확인 → harness <폴더>/ → 수정 → 재실행 → 통과
```

**복잡** (신규 섹터, 구조 변경):
```
maker-review-loop 사용 (Reviewer PASS 없이 sprint-close 금지)
```

---

## 완료된 스프린트

| 날짜 | 스프린트 |
|------|---------|
| 2026-08-25 | [플랫폼/섹터 구조 확립 + 바이브 키트 이식](docs/sprints/2026-08-25-structure.md) |

> ⚠️ 여기에 상세 이력을 쓰지 말 것. 한 줄, 상세는 아카이브에.

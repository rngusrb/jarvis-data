# BACKLOG

## 현재 상태 (2026-08-25 기준)

수집 파이프라인 가동 중. 수면·걸음수·휴식기 심박이 매일 아침 단축어로 들어온다.
플랫폼/섹터 구조 확립 완료, 경계는 불변식 테스트가 집행한다.
섹터 2개(health · commute). 테스트 129개.

| 지표 | 이력 | 수집 |
|------|------|------|
| `sleep_hours` | 85일 | 단축어 (기상 시) |
| `step_count` | 1,642일 | 접음 — 운동 중 표본이 하루 1,100개까지 가 단축어가 멈춘다 |
| `resting_heart_rate` | 102일 | 단축어 (기상 시) |
| `location` | 0일 | 아이폰 `떠날 때` 자동화 대기 |
| `work_departure` · `work_arrival` | 0일 | 위치 패턴이 보이면 켠다 |
| `heart_rate_avg` | 108일 | 접음 — 원본 심박은 하루 361개라 단축어가 버티지 못함 |

---

## 대기열

### 구조

- **발화 기억이 메모리에만 있다** — 서버를 재시작하면 자비스가 했던 말을 잊고
  같은 잔소리를 반복한다. 개발 중 재시작이 잦아 실제로 겪고 있다.
  `src/brain/memory.py` 의 `SpeechLog` 를 저장소 기반으로. 발화→반응 기록 자리도 함께
  (섹터 자기확장의 평가 신호가 된다)

- **`src/channels/` 전용 테스트 없음** — `test_loop` 가 `RecordingChannel` 로 간접 검증만 한다
- **`app/` 전용 테스트 없음** — 배선이라 불변식에 의존. 조립 결과를 확인하는 스모크 테스트 검토
- **`docs/sprints/` 아카이브가 1건뿐** — 이전 작업은 PR 본문에만 있다

### 수집

- **단축어 실패 시 재시도 없음** — 서버가 꺼져 있거나 와이파이가 끊기면 그날 데이터가 유실된다.
  현재는 백필 재실행으로만 복구
- **`app/main.py` 가 맥에서 돈다** — 덮개를 닫으면 자비스도 잔다.
  기상 자동화가 쏘는 시각에 맥이 자고 있으면 요청이 사라진다. 데스크탑(Ubuntu)으로 옮겨야 함
- **iMessage·캘린더 pull 수집기 미착수**

### 정확도

- **걸음수 수집 재개하려면** 소스를 워치 하나로 좁히고 그룹화해야 한다 (하루 1표본).
  워치 안 찬 날이 0이 되는데, 그날은 아이폰이 3표본에 40걸음을 잡는 수준이라
  "측정 안 됨"이 오히려 정직할 수 있다
- **워치 배터리가 낮에 죽으면 걸음수가 과소 집계** — 시간 구간 채택 방식의 알려진 트레이드오프.
  두 배 부풀리는 것보다 낫다고 판단해 수용 중
- **낮잠이 다음 밤으로 딸려간다** — 정오 경계 방식의 알려진 트레이드오프
- **`SleepDropTrigger` 의 NOTABLE 기준이 아직 절대값(1.5시간)** — URGENT 만 비율로 바꿨다.
  현재 데이터에선 결과가 합리적이라 보류

### 이동

- **`Observation` 이 숫자 하나를 전제한다** — 위치는 좌표 둘이라 `meta` 에 실었다.
  `value` 는 GPS 정확도로 썼는데 의미가 잘 맞았지만, 비슷한 데이터가 더 늘면 모델을 다시 봐야 한다
- **교통 도착 정보 API 미착수** — 서울/경기면 공공 API가 있다. 자비스가 **밖에 물어보는 첫 사례**가 된다
- **`떠날 때` 는 지오펜스라 몇백 m 여유가 있다** — 나서는 순간보다 조금 늦다.
  `Bluetooth`(에어팟 연결)가 더 이른 신호일 수 있어 보조로 검토

### 프로덕트

- **섹터를 데이터로 선언해 LLM이 새 섹터를 만들게 하기** — 손으로 2~3개를 만들어봐야
  공통점이 드러난다. 그전엔 뭘 생성할지 모르는 채로 생성기를 만드는 꼴
- **발화에 대한 사용자 반응 기록** — 텔레그램 인라인 버튼(👍/그만).
  자기확장에서 "줄이는" 판단의 유일한 신호가 된다
- **Qdrant** — 텍스트(메모·대화·관심사)용. 관측치는 시계열이라 SQLite 가 맞다

### 환경

- **로컬 Python 3.10 / CI 3.11** — 3.11 전용 문법을 피해 쓰는 중
